# this environment file is sourced from the Makefiles in this project

# bucket name and prefix path used to store templates, data, scripts and
# build artifacts
ENV_BUCKET_PATH ?= aws-bigdata-blog/artifacts/harmonize-search-analyze

# this name is prefixed to the ECR repos and CloudFormation stacks
ENV_NAME ?= datasearch-blog

# version used to tag docker images
ENV_VERSION ?= 1.0.0

# port used by nginx proxy to listen locally when running under docker-compose
LOCAL_PROXY_PORT ?= 8888

# AWS cli env variables used when running/building
# Override by setting it in the environment before running make
AWS_DEFAULT_PROFILE ?= default
AWS_DEFAULT_REGION ?= us-east-1
